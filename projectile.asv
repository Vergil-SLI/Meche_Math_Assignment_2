theta = pi/4;
t = 0;
max_t = 100;

t_c = -1;

% while t_c == -1 && t < max_t
%     t = t + 0001; 
%     projectile_traj-
% end

solver
multi_newton(@(projectile_traj-target_traj), [2], )

run_simulation(theta,t_c)